apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: compat-demo-clusterinterceptor-reader
subjects:
  - kind: ServiceAccount
    name: compat-demo-pipeline
    namespace: compat-demo
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: compat-demo-clusterinterceptor-reader